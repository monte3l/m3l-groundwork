# Harness refresh tracker

<!-- harness-refresh: last-verified=2026-09-24 claude-code-version=2.1.281 -->

Living record of `harness-guidance` (refresh mode) sweeps — the per-facet,
per-source state a run diffs against, so each sweep reports what
**changed** since the last one instead of rediscovering the whole `.claude/`
surface from scratch. Updated **in place** on every run, never as a new
dated file.

**2026-09-24 sweep** — first sweep, against this repo's own self-hosted
`.claude/` (root, installed by self-adopting `templates/core`'s harness —
see `CLAUDE.md`'s "Agent Operating Model"), running Claude Code 2.1.281.

## Outstanding drift

| Item                                                                                                                                                                                                                                                                        | Facet          | Status                                                                                              |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | --------------------------------------------------------------------------------------------------- |
| `Explore` agent's `effort: low` frontmatter field has no effect — Haiku 4.5 does not support the `effort` parameter at all                                                                                                                                                  | models-tiering | remediation plan proposed this sweep                                                                |
| Anthropic's current model-selection guidance now recommends Opus 5.5 for "multihour autonomous coding agents, large-scale refactoring, complex systems engineering" — squarely `code-implementer`/`code-reviewer`/`silent-failure-hunter`'s work, currently all on Sonnet 5 | models-tiering | flagged for a maintainer decision, not auto-applied (cost/behavior tradeoff, not a correctness bug) |

## Facet state

| Facet                        | Last verified | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---------------------------- | ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `models-tiering`             | 2026-09-24    | `claude-sonnet-5` model-id string and the `low`/`medium`/`high`/`xhigh`/`max` effort vocabulary are both still current. Real findings: (1) `effort` is silently a no-op on Haiku 4.5 (Explore's config) — correctness fix, not a preference. (2) Opus 5.5 (`claude-opus-5-5`, added Claude Code 2.1.280) is now Anthropic's recommended default tier for complex/multihour coding-agent work; Opus 5.5 defaults to `medium` effort (not `high`, unlike every other effort-capable model) — a real tiering decision, left to the maintainer rather than applied.                                                                                                                                                                                                                                                                  |
| `cc-features-settings`       | 2026-09-24    | Current `.claude/settings.json` shape (no `permissions` block, 3 hook events, `statusLine`/`subagentStatusLine`) remains fully valid under 2.1.281 — omitting `permissions` is still supported, not deprecated. Optional-only new keys surfaced: an `attribution` object (`{commit, pr, sessionUrl}`, not the boolean the CHANGELOG text implies), `syncClaudeAiSkills`/`syncClaudeAiPlugins` opt-outs, and two new `permissions` sub-flags (`blockReadsOutsideWorkingDirectories`, `disableBypassPermissionsMode`) — none required for a solo-maintainer OSS repo.                                                                                                                                                                                                                                                              |
| `agent-subagent-design`      | 2026-09-24    | Subagent frontmatter schema (`tools`, `disallowedTools`, `model`, `effort`, `maxTurns`, `mcpServers`, `permissionMode`) is current with no drift; newer optional fields (`memory`, `isolation`, `hooks`, `experimental.cacheTtl`) exist but aren't needed. The parallel read-only reviewer pattern (`code-reviewer` + `silent-failure-hunter` over the same diff) matches Anthropic's own documented subagent guidance. The hub-never-writes-directly pattern (`guard-hub-src-writes.mjs`) is a sound, hook-documented mechanism but is this repo's own design, not an Anthropic-authored best practice — correctly treated as a local convention in `CLAUDE.md`, not restated as upstream authority. `maxTurns: 40` uniformly is a reasonable, conservative default; no evidence of truncation to justify per-agent tuning yet. |
| `skills-context-engineering` | 2026-09-24    | The 1,024-character `description` limit and the progressive-disclosure (`SKILL.md` → `references/*.md`) pattern are both current and already followed exactly by `typescript-guidance`/`harness-guidance`. No frontmatter mechanism exists yet to enforce "hub-only, never in a subagent" — both skills' prose declaration remains the only option; nothing to change.                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `hooks-lifecycle`            | 2026-09-24    | The 3 event types in use (`UserPromptSubmit`, `PreToolUse`, `PostToolUse`), the tool-name matcher, the `if:` permission-rule-syntax conditional matcher, and the exit-code contract (exit 2 blocks on blockable events; on `PostToolUse` — non-blockable — exit 2 delivers advisory feedback only, exactly how `post-edit-verify.mjs` already uses it) are all confirmed current with no drift. 30+ additional lifecycle events exist upstream (`Stop`, `SubagentStop`, `PreModelSwitch`, `WorktreeCreate`, etc.) and are available for future guard ideas, but none are required now.                                                                                                                                                                                                                                           |
