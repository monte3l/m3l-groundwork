node_modules/
dist/
coverage/
pnpm-lock.yaml
CHANGELOG.md
# Prettier normalizes Markdown `__emphasis__` to `**emphasis**`, which would
# rewrite the intended bold __PROJECT_NAME__ token back into
# ****PROJECT_NAME**** on every `pnpm format` run in this repo. The emitted
# project's own .prettierignore does NOT carry this exclusion -- once
# emitTemplate() substitutes the token for a real project name, formatting
# that file is harmless again.
templates/core/CLAUDE.md
